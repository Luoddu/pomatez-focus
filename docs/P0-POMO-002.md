# P0-POMO-002 — 三机轮用成果同步

状态：REVIEW（本地实现/隔离验收通过，真实历史回填待首次启动）；角色/负责人：独立番茄模块实现者 codex；状态源为本卡。

## 授权与范围

用户认可三台电脑轮用，同一飞书表共享已保存成果，不接力计时，并明确要求推进执行。基线 bcea6e67cbf1197e18a1633cbb2ae0ac9e9ac122。继续在隔离模块工作树实施，不部署其他电脑，不写根共享状态。

允许：app/electron/src/focus/ 下同步实现、main/preload 的受限 IPC；renderer focus/ 与 CounterContext；相关测试、构建版本、README、FEISHU/验收/本卡；本机隔离测试产物。现有表增加可隐藏文本列“跨端专注记录”，复用 Feishu 现有字段与记录 API。用户本机历史先备份，补传已保存记录；不重新累计已同步分钟，不改任务完成，不删除云端或本地历史。凭证仍本机加密，不复制到 Git 或跨机文件。新包与公开源码沿用既有发布授权；不含私人素材。

非目标：跨机倒计时、同时多机写同一番茄的原子合并、新服务器/任务表/云平台、远端机器安装、NAS 同步、根仓治理/架构修改。

## 设计

原分钟台账保持原有兼容格式；新列单独保存带唯一 ID、开始/结束、计划/计时/确认秒数、用户完成数、任务与象限快照的不可变记录。按 ID 去重，内容冲突报错，拒绝覆盖。新专注保存后上传/下载；启动与手动同步也拉取。当前倒计时不参与合并。旧记录补传只写新列；尚未上传的本地自由记录通过原自由番茄流程入表。同步分批提交本机成功状态，断网保留待处理；失败不得显示全部成功。旧云台账缺少完整快照时提示需在原电脑补齐，不从 0/1 猜用户数。

## 上下文与四处自检

1. 当前代码：CounterContext 的 records 在 localStorage；Feishu plan ledger 的 count 为 0/1；sync receipt 刻意不覆盖本地 completedCount。旧数据不能直接复原完整成果。无其他活动写者；开发/发布旧分支干净。
2. 官方：沿用既有固定 Electron 34.5.8、SDK v1.6.8 请求结构和当前 Feishu GET/PUT records、POST fields 接口；不升级依赖、不引入新 API/服务。SaaS 版本不可冻结，既有调用本机已验证，新增内容仅业务字段与 JSON。
3. helper/Learning：现有 task-commit、verify-staged、planQueue、plan fixture、隐藏桌面/打包检查可复用。此前打包 npm 重装问题已查明，采用官方 npmRebuild=false；不更改锁文件。
4. acceptance：复用 73 个逻辑测试、21 个桌面检查及专门手动同步测试，新增 A/B 共用模拟飞书、迁移不重复记账、离线/冲突/跨 Base/当前计时保持等验收。

必须加载：本卡、治理版本控制与派发要求、模块同步/计时源码及测试。条件加载：真实数据迁移前只读备份与摘要、发布前隐私扫描。明确不加载：其他项目/人的隐私与根无关架构、其他机器凭证。越出上述边界暂停对应步骤，不扩展任务。

## 资源、预算与验收

独占：当前模块 worktree 与 task/p0-pomo-002-multidevice；测试随机 profile，无监听端口/长期服务；不停止用户活动计时。子 Agent 0；不创建后台自动化。一轮实现及有针对性的失败修正、构建/模拟与真实隐藏桌面验证；同根因未知失败两次暂停。提交/发布前核对工作树、HEAD、远端，仅推送本任务已检查路径。

正反验收：A 保存 B 拉取且同 ID 不重复；B 再保存 A 合并且 A 进行中计时不变；补传用户完成 3 个保留 3 且分钟/已完成不二次写；离线保留记录且恢复可同步；跨 Base、同 ID 改值、畸形云数据拒绝且合法记录可正常读写；空待传队列仍可下载；旧台账无快照需提示而不造数。三台必须升级同版、换机前同步完成，首版不承诺并发修改同一行安全。

回滚：保留 preview.17 与旧本地导出；新增列不删除，旧版不识别也不改该列。不得通过回滚覆盖已保存成果。Learning 暂无新增通用规则；检查点按证据记录候选或 none。
允许写集补充：scripts/package-focus.cjs 显式复用 electron-builder 25.1.8 的 npmRebuild=false，避免已知无运行依赖应用的 workspace 重装；新版本号 preview.18。

交付补充：更新已确认的本机桌面快捷方式目标便携 EXE，保留一份 preview.17 回滚文件；不停止当前 App。公开验收见 docs/ACCEPTANCE-multidevice.md。

## preview.19 兼容修复（2026-09-13）

用户截图反馈剩余四条待共享。沿用本卡范围，基线 57a990c；当前分支干净，无重叠写者。先四处核对：① 自动备份与真实云端只读对照：18 条中 14 条已共享，两条旧 taskId 为空字符串，两条读取原计划行失败；② 纯业务快照校验，不改变第三方运行语义，官方机制变更不适用；飞书 get record 错误含 1254043，需明确原行缺失而非泛称权限错误；③ feishu.ts:444 原生产路径使用空字符串表示缺失关联，复用隐藏 helper 和既有元数据核对，不重建行；④ history/cloud/desktop 原验收缺少旧空字符串样本，补正反回归。

修复仅兼容可选 planId/taskId 的空字符串（保留原表示，避免合并冲突），非空仍严格检查类型与字符；原行不存在仍阻止写入并给出明确提示。允许写集补充：preview.19 版本与 Release 文档、对应测试。验收：合法旧记录可共享且不重复计账；非法 ID/不存在原行仍拒绝且零写入。本机旧记录只读核对，不自动重建删除的计划行，不停止活动 App。预算一轮局部修复、构建、回归和发布；子 Agent 0、无新后台服务。回滚保留 preview.18 EXE；本地与云端记录不删除。

## 两条原行缺失记录的一次性手动补传（2026-09-13）

用户本轮明确要求直接手动传到飞书、简单解决。以 ed54275 为基线，继承隐藏运行、备份、幂等和凭证边界；本次特许仅为已定位的两条旧会话创建原番茄表内的历史补录行（历史日期、原任务关联、已确认分钟和完成、原 UUID 跨端快照），不创建表、不改已有行、不删除记录、不改变产品正常的原行缺失拒写策略。原快照的关联字段不变，下载按 UUID 去重。

四处核对：① 两条原行缺失，自动备份与旧会话合计 3129 秒/2 个番茄；② 复用已验证 Feishu POST records + client_token，不改变接口/SDK版本；③ 复用 mergePlan/mergeHistory/sessionFields/fieldsAgree 和隐藏 helper；④ 原 86 项回归与三进程合并验收继续适用，本次增加真实只读预检→精确两次创建→GET读回→全表UUID唯一性与汇总核对。脚本写入守卫只允许事先核验的两个字段体与确定性请求令牌；错误不盲重放。

执行资源仅本机私有 helper 与两条历史行，不停止 App，不改本地活动存储。回滚凭私有备份内新行回执精确定位（不自动删除），原数据不变；隐私脚本/回执不进入 Git。无子 Agent/新服务/版本打包；本次只做数据补传与验收回执。
