# 番茄农场 preview.42

公开源码 `549ccd2f03aa4eb02e5470cb99687beaaa1ec73a`（codex/feishu-focus），
在 preview.41 之上叠加农场体验增强。

- 丰收筐重做：果堆随累计收获逐层长高（上限 21 颗六层堆尖），新番茄弹跳入筐（既有果实不重播）；累计 30/100 颗两侧各多出一只小筐；超量显示金色「+N」徽标；50/100/200/300/500/1000 颗堆尖挂金星级程碑旗。
- 季节时令：按北京时间月份换景——春飘花瓣、夏来蜻蜓、秋落红叶、冬落雪且土壤带覆薄雪、稻草人戴红围巾；减动效偏好下全部静止。
- 番茄果实保持哑光无高光点的仓库验收约束，渐变压深至 16% 边缘暗部。
- 专注记录：同步状态圆点徽标（绿=已共享到飞书、橙=待同步/待共享、灰=本地记录）；「修改」改为铅笔图标按钮，默认收起、悬浮该行滑出（触屏与键盘焦点始终可见）。
- 概览：今日番茄沿用日合计成就分级（≥5 番茄红、≥10 金色弹跳）；总番茄新增里程碑进度条与「已达成 N · 距 M 还差 K」（10/25/50/100/200/300/500/1000 档，满档「🏆 丰收传奇」）。
- 新增 `seasonOfMonth`/`beijingMonth`/`totalMilestone` 纯函数与 11 项单测；`scripts/shot-seasons.cjs` 支持四季 mock 截取与记录行悬浮验证；`tests/verify-update-desktop.cjs` 用真实 GitHub Provider 端到端验证更新检测。

Windows x64：下载 setup.exe 安装版或 portable.exe 便携版。正常退出旧版后运行新版，沿用原应用数据目录、飞书配置与历史记录。本次没有数据迁移，不需要复制凭证文件。

验收：156 项单元测试通过；98 项界面检查通过（含哑光番茄、堆形确定性、天空实时性）；真实 NsisUpdater（GitHub Provider、channel preview）从线上 Release 检测到 0.1.0-preview.42 并取回 setup.exe 元数据，无错误。校验文件 preview.yml（SHA-512）随包发布。仅验证 Windows x64，仍是未签名预览版。
