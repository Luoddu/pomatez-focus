# P0-POMO-024 专注积累最新在上

- 状态：IN_PROGRESS；隔离模块实现者与本卡负责人 Codex。用户授权调整展示顺序。
- 基线 ad4bb55（origin/codex/feishu-focus）；独立 task/p0-pomo-024-history-order 工作树。开工已核对干净状态、worktree 列表与实时 fetch，无重叠未提交写集。
- 目标：日期倒序，同日记录倒序，今天首先显示；横竖屏均从上往下查看。保留历史遮罩、展开、补记、编辑、同步及统计。
- 允许：HistoryPanel.tsx、focus.css、已有 portrait-desktop 测试、本卡及验收文档；交付时既有版本文件、CHANGELOG。独占本任务工作树和合成测试 profile。
- 禁止：真实飞书/记录修改、凭证、用户 App 重启、根共享文件、新依赖、其他业务逻辑。
- 四处预检：① HistoryPanel 已按 startedAt 降序分组，但渲染 reverse 再反转；旧竖屏 CSS 为横向日卡，横屏仍纵向，导致今天排底部。② 纯自有展示修正，不改变第三方语义或版本，无第三方研究要求。③ 复用既有分组、记录展开、测试 runner；根 Learning 定向检索排序/倒序无命中，模块无 Learning 文件。④ portrait-desktop 原用例明确断言今天在右，需按新需求更新；category-review 桌面验收覆盖折叠/展开。
- 验收：横竖屏今天在顶、昨日次之、同日较晚记录在先；旧日补记仍归原日、不同输入顺序不改结果；历史可展开、记录可编辑且数据不改变；查看旧记录时同步不强制拉回顶部。构建与相关桌面回归通过。
- 上下文：只加载模块规则、直接代码及测试；不加载真实数据和无关控制面。无子 Agent/后台监控；单轮局部修改，90 分钟时间盒；同因未知失败两次停对应尝试。关键修改和提交前复核工作树/远端。
- 回滚：回退本任务独立提交即可；无数据迁移。Learning：none，本次是已有展示方向与需求不一致的局部修正，无新的跨任务工程结论。

## 交付检查点
功能与回归已验证，见 ACCEPTANCE-P0-POMO-024.md。沿用用户本会话既有 GitHub 源码与安装包一起发布授权，交付范围为 Luoddu/pomatez-focus 的新 preview.59 Release、六个原格式附件与更新元数据；不安装/重启用户 App。已补读根发布/版本边界，无根写入。
