# P0-POMO-023 分类回顾与历史遮罩

- 角色：番茄农场独立模块实现与验收；状态：DONE（功能与发布已验；临时加密检查配置清理被自动审批拦截，记录为例外）。
- 基线：preview.57 / 7ff1cd18e4bbf6620283ec0634f77a0fba9065b6；分支 task/p0-pomo-023-category-review。公开主线实时核对一致；工作树干净、其他工作树没有本任务写集。
- 目标：已保存记录右键手动分类；历史日卡默认毛玻璃遮住明细，展示真正复盘摘要，点击才展开；每日收获可按分类堆叠；专注趋势按分类切换并保留完整 7/28 日观察窗；已保存记录独立修改结束时刻；补记可以输入临时名称。
- 范围：本模块 app/{electron,renderer}/src 下 focus、main/preload、CounterContext（若必要）、相应 tests、scripts/shot-app.cjs 与隐藏测试包装脚本、package.json 测试命令、docs 本卡/验收及既有版本位。仅本工作树构建、临时隔离配置与模拟数据为独占资源。
- 禁止：根共享控制面、真实专注计时状态、真实记录批量改色、凭证输出/复制、新表、飞书分钟或完成状态写入、删除历史、覆盖其他工作树或强推。用户已授权只读查找现有飞书复盘；开发测试不向真实飞书写入。
- 四处自检：① 现有记录 task.projectType 为不可变快照，mergeCloudRecords/mergeHistory 拒绝差异；dailyFocusTheme 只是任务名称摘要，不是真正复盘。② 复用固定源码基线已有 Bitable tables/fields/records 路径、分页、App safeStorage 与串行 service，不升级第三方包。③ 复用 EditQueue 持久意图、historyRecord 白名单、7/28 日趋势与颜色表；没有模块通用新 Learning。④tests/history/cloud/edit-queue/stats/week 与隐藏桌面、竖屏验收已有同类证据。
- 正向验收：每条记录手动类别在重启和同表同步后保持；恢复项目默认色可用；灰番茄有明确入口；缩短跨夜结束时刻保留暂停、身份、已确认时长，修改起点平移原时段；临时名称直接补记；遮罩点开与收起、鼠标/键盘、横竖屏正常；分类柱总量守恒，零番茄有时长仍计趋势，类别无投入日计零。
- 反向验收：仅颜色元数据可写，任务关系、时段、分钟、完成状态和数量不变；重试不重复增版本；同版本冲突不覆盖；完整观察窗口与全历史同基线，不能因分类少而缩水；收起时不把具体任务名挂在标题或 tooltip；摘要缺失不猜造；结束早于起点、未来时间或确认时长超过实际片段时拒绝，正常时段仍能保存。
- 上下文预算：只读当前模块相关调用链/测试与用户提供截图；不读取无关主线材料。无子 Agent、无后台监控；临时进程隐藏且有超时；真实接口身份失败不盲重试。
- 执行：读调用链和复盘 schema；实现独立颜色元数据及持久重试；遮罩/分类视图；纯函数与模拟服务测试、隐藏 Electron 验收；按已有发布授权准备新版本、干净提交/构建/打包/非强推/新 Release。正在使用的用户 App 不切换。
- 回滚：独立提交、旧发布物保留；颜色元数据可恢复项目默认，不改原项目快照；复盘仅读取，不写回。
- Learning 判断：局部交互与既有协议扩展，无跨任务验证的新通用教训；不把本任务状态写入根 Learning。
