# P0-POMO-002 — preview.18 多电脑成果验收

## 已验证

- 生产构建通过，固定 Electron 34.5.8 / electron-builder 25.1.8；无依赖升级。
- 83/83 逻辑及模拟 API 测试：原有 73 项与新增 10 项，覆盖往返去重、保留用户多个番茄、旧台账缺失提示、只补快照不重放记账、自由记录幂等、断网重试、旧会话核验、任务关联改变、跨 Base、内容冲突和畸形云数据。
- 21/21 通用隐藏桌面检查与独立进程重启恢复通过；4/4 手动同步检查通过，空上传队列仍可下载、忙碌期间不可重复请求。
- 三个真实 Electron 进程共享模拟飞书，各自独立 profile：A 补记 2 个 → B 自动读到 → B 当前计时中手动获取另一条记录且 active ID/status 不变 → B 补记自由番茄 → A 再启动共 3 条记录/4 个番茄；重复同步无新增。
- 第一轮迁移前自动备份文件存在，原本地记录保留；并未从云端 0/1 达标数推测用户完成数。
- 真实飞书只读兼容检查通过。仅新增并回读“跨端专注记录”文本字段，写入目标由私有 helper 限定为该字段定义；未改实际番茄行的分钟、完成或任务。
- 包内 58 个路径、33 个文本文件通过个人名称/路径检查，MIT LICENSE 与源码一致。解包版输出 hidden ready 并退出 0，便携版隐藏启动退出 0。

Windows portable SHA-256：3b29ffbbee8b7ddfb913d58f5c58782b8b0f38d70726968f8ddc085ed10c77bc；75,143,698 字节。

## 如实保留的边界

- 用户主力 App 仍在运行，未终止或重启它。真实本机历史的备份与逐条补传由新版首次启动执行；本次不声称已完成真实历史迁移、或三台物理电脑实机终验。
- 其他两台电脑需安装相同版本并各自配置飞书。首版要求轮流使用、换机前同步完成；前置读回检查不能替代飞书服务端 CAS，不承诺并发写同一行的原子合并。
- 当前计时不上传；云端缺少完整历史快照、原番茄已删除、冲突或单行容量超限会提示，保留本地，不造数、不删除、不重建原计划行。
- 切换飞书表不会把另一表的历史上传到当前表；本地已有其他表记录仍保留。未绑定的本地自由记录在当前连接下按自由番茄流程入表。
- 全量 renderer tsc 4.9 检查遇上游 i18next TS5 声明语法不匹配；没有升级或绕过依赖，使用既有正式 CRA 构建验证通过。一次新测试使用 ES5 不支持的 Map iterator 展开，修正为 Array.from 后构建通过。
- 最初隔离 profile 缺少原 Local State 导致本机凭证解密失败；按已有只读 helper 使用原 profile 的 safeStorage 后成功。未迁移凭证、未重新授权账号。
- 部分隐藏测试退出时有 Chromium GPU 清理诊断，没有启动/计时/同步断言失败。

## 复跑与回滚

执行 README 中构建、逻辑、桌面命令及 node tests/cloud-desktop.cjs。模拟测试不访问真实账号。

保留 preview.17 可执行文件和首次迁移备份，回退版本不得覆盖用户现有记录；新增云字段保留。新包仅作为 Release 资产，不进入 Git。

Learning：本次用既有同目录加密上下文、隔离测试和固定版官方打包配置解决问题，没有新增通用规则；不写入根共享 Learning。四处自检与写集见 P0-POMO-002.md。当前为待主力机首次迁移与其他设备体验的模块交付检查点，不冒充跨机实测 DONE。

## preview.19 旧快照兼容修复

已验证：从自动备份与真实飞书只读对照，18 条记录中已有 14 条共享；未共享的 4 条分别为 2 条旧 taskId 空字符串和 2 条原计划行读取返回 1254043。旧 task 生产路径 feishu.ts 使用空字符串占位，而 historyRecord 误要求可选字段至少 1 个字符，是本次应用校验根因；不属于网络或权限机制故障。

修复保持空字符串快照不变，避免 renderer 不可变关联比较产生二次冲突；非字符串、空白、路径字符、超长 ID 仍拒绝。原行不存在时改为明确恢复提示，API 其他错误继续透传。未创建替代计划行、未增加重复分钟或修改完成状态。

86/86 逻辑/模拟测试通过。新增旧会话空 taskId 上传/下载/重复同步及 renderer 合并测试，检查其余原表字段未变；新增非空非法 ID 和原行不存在零写入反例，并验证原行恢复后可同步。修复后对真实四条重做只读核对：两条兼容记录通过旧会话一致性验证，到达元数据 PUT 前由私有 helper 主动阻止；另两条仍提示原行不存在。没有实际写入用户记录，也未重启用户 App。

四处自检、基线、范围与回滚见 P0-POMO-002.md 的 preview.19 段。Learning: none - 本次是项目特有旧快照空字符串兼容回归，既有不可变合并、隐藏运行和备份机制保持，不形成新增跨任务规则。

preview.19 包验收：正式构建/portable 打包通过；A→B→A 三个独立隐藏 Electron 进程往返通过，当前计时保持；解包版 hidden ready/exit 0，portable 隐藏启动 exit 0。包内 58 个路径、33 个文本隐私扫描无命中，MIT 精确一致。EXE 75,144,202 字节，SHA-256 `6f60e13aca20d05a0344e3fbf3650ebd5b2267b5d75e6b52925d41d997b67296`。沿用桌面入口，保留 preview.18 回滚；用户需托盘退出旧进程后运行新版，真实补传仍待 App 执行。
