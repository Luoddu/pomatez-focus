# P0-POMO-025 编辑结束时间联动时长

- 状态：REVIEW（实现、回归、preview.60 安装包发布已验证）；隔离模块实现者/上下文负责人 Codex，用户已明确授权执行。
- 基线：264f7791f27e4df55ff9c159da12423f3c0fdd14；task/p0-pomo-025-end-time，独占本工作树与合成测试 profile。已 fetch 并核对工作树，无重叠写入。
- 目标：缩短结束时间时自动将超出的已认可时长调到可用范围，显示扣除暂停后的可计入上限；番茄数保持用户选择。延长结束时间不自动增加认可时长，仍可手动微调。
- 允许写集：manualRecord.ts、ManualEntry.tsx、manual-category.test.mjs、category-review-desktop.cjs，本任务卡/验收、CHANGELOG、既有两个版本文件。禁止真实记录/飞书写入、凭证、用户 App 重启、根控制文件、新依赖和协议改造。
- 四处预检：① 原 UI 修改结束时间未更新 minutes，保存却按裁剪 segments 验证，含暂停时尤其易触发。② 自有状态和表单逻辑，无第三方关键语义变化，不适用额外官方调研。③ 复用 manualRecord、纠正队列、隐藏桌面 runner；根 Learning 此前定向检索无相关命中，模块无 Learning 文件。④ manual-category 与 category-review-desktop 已覆盖裁剪/跨日/同步纠正，扩充自动联动及有效上限回归。
- 验收：短时段不能保存超额，但自动调整后可保存；暂停不计入，番茄数量和记录身份不变；延长不虚增，合法小数分钟与手动减少可保存；无效时间仍提示；原补记联动、跨日平移和纠正同步仍通过。
- 上下文只加载模块规则及直接代码测试；发布沿用既有用户授权、既有 GitHub 六附件流程，复核实时远端版本；不安装用户 App。无子 Agent/后台监控；90 分钟时间盒，同因未知失败两次停止该尝试。修改/提交/发布前红黄绿核对。
- 回滚：回退独立提交，无数据迁移。Learning 判断：none，既有表单与验证口径不一致的局部修正，无新的跨任务机制结论。
