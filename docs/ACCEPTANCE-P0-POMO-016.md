# P0-POMO-016 验收回执

- 状态：本机隔离工作树已验证；本次获准推送源码，未发布 Release 或替换用户正在使用的安装版。
- 源基线：`origin/codex/feishu-focus` / `a8cf4835eeddee1a9c1784ba58f84bc7838195ae` / preview.49；本任务分支 `task/p0-pomo-016-stats-curve`，源码版本准备为 preview.50。
- 统计：保存记录的分桶绘制实际收获曲线和过去最多三期移动平均；当前区间只到已发生的日期。曲线卡采用浅色底，实际收获为珊瑚色、移动平均为紫色，并调整网格、坐标与图例对比度。五项汇总改两列，1080 宽竖屏下按走势、汇总与柱图/热力、分布卡组织。
- 主页：月历格子竖屏扩大；未到达的小旗仍直立。里程碑从已达成档计起，例如累计 129 时显示 100→200，当前数字位于进度 29% 处。农场删除无说明的果筐溢出计数与“周日翻篇”。计时与休息横向对齐；统计和生成今日番茄为可辨认的按钮。记录从左旧到右新、默认最右、三列横向浏览，今天轻微着色且记录卡撑满剩余高度。
- 正例：`npm run test:focus:all` 201/201（含统计纯函数测试）；`npm run build:focus` 成功；`npm run test:desktop` 横屏 21/21、竖屏 13/13；隐藏窗口隔离演示 `scripts/shot-app.cjs` failed 0。实际浏览检查 `artifacts/ui-shots/app-stats-week-portrait.png` 与 `artifacts/portrait-preview.png`，使用模拟任务与隔离配置。
- 负例：进行中记录不计入趋势，未来日期不绘制，原统计和分享入口保留；无真实飞书写入，未触碰安装版、配置、凭证和历史数据。横屏/竖屏测试均未弹出可见窗口。
- 回滚：撤销本任务分支中列于执行卡的改动；原 `task/farm-delight` 工作树及其未提交变更保持原样。
- Learning 判断：该改动是局部视图排版与明确的数据口径，尚未产生经跨任务验证的通用工程教训，无 Learning 候选。
