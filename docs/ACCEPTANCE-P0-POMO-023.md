# P0-POMO-023 验收回执

- 状态：功能与本机隔离链路已验证，正式运行数据未修改；preview.58 已公开发布。
- 发布：[v0.1.0-preview.58](https://github.com/Luoddu/pomatez-focus/releases/tag/v0.1.0-preview.58)，源码/标签/包内来源均为 43d9c1250ea152b2be5af82e60fc8acd42412ea1，干净构建和安装/便携包完成；GitHub 六个附件的大小、SHA-256 与本地逐一匹配，更新元数据 SHA-512 与安装包相符。打包应用与便携启动器均用独立配置通过内部隐藏启动检查，原用户进程仍在运行。
- 基线：preview.57 / 7ff1cd18e4bbf6620283ec0634f77a0fba9065b6；独立任务分支 task/p0-pomo-023-category-review，版本 preview.58。未修改其他工作树或根共享控制面。
- 结束时间：保存记录的修改窗口同时提供起点、终点、实际专注分钟和番茄数。端点未变时保留原片段；缩短终点裁剪片段、扣除暂停；移动起点会平移原片段。确认分钟不能超过裁剪后的实际片段，不从跨夜墙钟跨度补造专注。身份、任务和已确认番茄数保持；合法修改走原显式纠正与回读协议。
- 补记：未选任务时可输入临时名称（最多 120 字），留空保留自由番茄；起止时间可分别填写，走原记录/共享机制。
- 分类：记录右键或 Shift+F10 展示六类和恢复项目默认。手动类别独立于不可变项目快照，使用颜色版本和持久意图，失败重试不重复。农场、记录图标、分类柱和趋势读取同一有效类别；四象限按钮仍按象限颜色。共享字段有手动颜色版本时采用 v2 封装，防止旧版重写丢失颜色；未修改颜色的旧记录兼容 v1。三台电脑应先升级到 preview.58 再使用手动分类，旧版不能继续写入 v2 历史字段；不能通过手改 JSON 降版。
- 复盘：用户授权只读检查确认已有日周期复盘可读。App 从现有安排与复盘表读取日周期的周期开始和进展与卡点，按北京时间日期关联；周/会话条目排除、重复日摘要不任选。缓存按表来源隔离；缺失与读失败明确区分，不猜造摘要或把任务名当复盘。过去日卡默认玻璃遮罩，收起时不渲染明细、任务主题和任务 tooltip；今天保持直接查看。
- 统计：仅保留既有四块。每日收获可切换总量与分类堆叠，分段悬浮提供数量、比例和时长；各段总量和秒数与原区间一致。趋势按钮选各类别时采用全部保存历史的观察起点，真实计算 7/28 日历日均，缺少完整窗口不画该均线，已观察的零投入日计零；浅暖底和番茄分类色。
- 单元/模拟接口：npm run test:focus:all 219/219，包括跨夜时段/暂停、移动起点、无效结束时间、临时命名、颜色版本冲突、丢响应幂等重试与恢复默认、元数据唯一写集、旧客户端兼容、分类守恒、完整均线窗口与复盘来源隔离。
- 隐藏桌面：21 项原生窗口/计时与实际进程重启通过；14 项竖屏/横屏布局通过；5 项真实 main/preload/renderer 到模拟 Feishu adapter 的新增链路通过；3 项原共享专注链路通过。隔离演示 scripts/shot-app.cjs 全部检查通过、failed=0。界面视觉证据在 artifacts/category-review-\*，全部使用隔离配置与模拟记录，测试窗口未显示。
- 正向：共享改色重启仍在、结束跨夜纠正与名称实际保存、玻璃点击展开/收起和键盘菜单可用、四象限与紧凑小窗原功能正常。
- 反向：改色唯一 PUT 字段为跨端专注记录，所有原分钟/完成/关联等不变；陈旧或同版本冲突拒绝；折叠 DOM 无具体任务名；非法时段拒绝；真实飞书写入为 0，未停止或升级正在运行的用户 App。
- 四处自检细节见 TASK-P0-POMO-023.md。官方机制参考固定 Electron v34.5.8 safe-storage 文档和 electron/electron#39733，仅读取本机既有连接作授权 schema 检查，未导出明文凭证。
- 清理例外：自动审批两次拦截本轮临时检查加密配置的删除（包含精确文件版），未给出进一步原因；停止清理，残留不纳入 Git/发布包。原用户配置未修改。
- 回滚：旧 preview.57 安装包继续保留；未使用手动分类的记录兼容旧版。已采用 v2 的共享历史应保留新客户端，不手改降版；可在新版恢复项目默认分类。真实运行配置/凭证/历史不参与代码回滚。
- Learning 判断：本轮为既有协议与局部交互扩展，已有元数据/幂等路径得到复用；未形成跨任务验证的独立通用教训，无根 Learning 候选。
